// Conformance NEGATIVE: `capability { ... }` is NOT accepted source syntax (M3
// source-level non-goal) — the parser must reject it, not silently ignore it.
procedure pseudo_capability {
    trigger t
    capability {
        field = "value"
    }
}
